# Copy this file to .env.production (gitignored) before running
# `npm run ios:build` on your Mac — the iOS app is built locally, not
# through Vercel, so it needs its own copy of this variable pointing at the
# real backend (never localhost).
VITE_API_URL=https://exportflow-api.onrender.com/api
